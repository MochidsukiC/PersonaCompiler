# DEVモード

システムプロンプトを同じ世界条件で比較するための実験モードです。新形式の実行で明示的に有効化します。通常の実行や旧形式のワールドには自動で追加しません。

## 操作

1. 上部の「DEV」→「DEVを有効にする」。都市・住民生成前へ戻りたい場合は、最初の資料送信前に有効化してください。
2. 親Agent・住民・施設・記憶照合のsystem instructions、記憶整理・Character Compilationの依頼文を編集します。
3. 推論中なら一時停止し、「保存して適用」。適用済みの全文をrevisionとともに保存し、既存Conversationを新しいinstructionsで再接続します。故人は再接続しません。施設・住民の次のSession生成にも適用します。
4. 保存地点を選び、「この地点から実験分岐を作る」。処理中なら停止を確認し、世界・Conversation・記憶・生成ファイルをその地点へ戻した新しい実行を作ります。
5. 内容を確認して準備・生活を再開します。終了地点へ分岐した場合は、DEV画面の「この世界からCompilationを生成」で新しい出力を作れます。

未適用の編集がある間は分岐できません。先に保存して適用してください。既定では適用済みの最新版を使い、「保存地点の版へ戻す」も選べます。住民のテンプレートには `{{townName}}` / `{{birthModelId}}`、施設には `{{townName}}` / `{{facilityName}}` を使用できます。置換は文字列置換だけで、コードを評価しません。JSON Schema・同意・Tool実行主体・保存先の検証はHarnessが保持します。

## 保存地点と復元

- DEV有効化時。
- 都市・仕様の生成要求前、質問回答待ち、都市・仕様レビュー。
- 承認後の住民生成前、全Sessionを作成した後の施設配置・生活初期化前。
- 生活開始前、朝・昼・夕・夜それぞれの時間ターン終了時。日次境界は加齢・死亡・出生・通知・記憶整理・Compactを解消してから記録します。

連続実行も内部では1ターンずつ確定保存し、保存成功後に次へ進みます。DEVには追加のディスク保存とConversation照会のコストがあります。未有効化の実行にはこの処理を追加しません。

有効化前や未確定の推論途中へ戻ったことにはしません。推論途中で停止した場合は、最後の確定した保存地点から分岐します。生成直前の未送信依頼は文章と画像の相対参照を保持し、明示的な再開で送信します。世帯・出生予定・記憶の保持／忘却・方向別の関係・Tool結果・客観履歴も保存地点にそろえます。生活・記憶の乱数seedは維持します。モデルの確率的出力そのものの再現は保証しません。

分岐後も祖先の実験のチェックポイントを選択できます。元の実行とその後の履歴・成果物は元のフォルダーへ残します。分岐元の未来の生成ファイルを新しい実験に混ぜません。

## Codexのrewindとの対応

[App Serverの公式仕様](https://developers.openai.com/codex/app-server/)の `thread/fork` に `lastTurnId` を渡し、指定された完了推論ターンまでを新しいConversationへコピーします。元Conversationを変更する `thread/rollback` は使いません。Codexの推論ターンと世界の時間ターンは異なるため、世界の境界ごとに全Sessionの到達地点を記録します。

まだ推論のない、初期条件だけを注入したSessionは、最初の保存時に推論なしの固定forkを作ります。その固定forkから分岐することで、後から元Sessionに増えた履歴を取り込みません。Compact後に過去へ戻す場合も、保存した `lastTurnId` より後の履歴・Compact結果を除外します。localhost Responses fixtureで確認しています。

## 永続化と失敗

`<runId>/dev/checkpoints/<id>.json` に保存地点・Conversationの到達地点・ファイルhashを記録します。`dev/blobs/<hash>` に内容を保存し、同じ内容を共有します。保存Workerの交互snapshotと、その地点までの確定history segmentsを固定するので、後の通常保存で巻き戻し元が上書きされません。内部blobは通常のファイルツリー監視から除外します。

分岐先はHarnessが作成したUUIDの実行フォルダーだけです。パス・実体パスとhashを検証し、元のworkspaceへのリンクをコピーしません。全forkの結果を記録した後にactive-runを切り替えます。各fork・プロンプト適用の前にdirtyを確定します。応答不明時は停止し、自動で再送しません。成功した途中の分岐データも削除しません。

異常終了の元実行は閲覧専用を維持します。DEVの既知の確定地点からの明示的な分岐は可能ですが、元の未確定推論を復旧・再送しません。Codexの履歴が別途削除されていた場合や、保存ファイルのhashが一致しない場合は復元を停止します。

## 検証範囲

Core／Backend fixture、Electron E2E、実Codex App Server＋localhost Responses fixtureを使用します。後者はseedのみのfork、Compact前へのrewind、未来の発言の除外、dynamicToolsの継承、baseInstructionsの適用を確認します。実モデルでの人格変化・指示追従品質は通常検証に含めません。
