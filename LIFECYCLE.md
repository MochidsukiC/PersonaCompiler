# 世代交代とCharacter Compilation

新規ワールドは`lifecycleVersion: 1`を保存します。旧ワールドへの自動移行はありません。初期人口は保持し、現在の年齢・世帯・家族・生死を住民台帳へ保存します。

## 時間・結婚・出生

朝・昼・夕・夜の4ターン終了ごとに1歳加齢し、80歳以上で老衰します。初期状態で80歳以上の住民も最初の日次境界で死亡します。先天的モデルは変更せず、Auto effortは現在年齢で解決します。

`marry`の引数は`partnerId`、追加希望人数`children`、子の世帯となる親`homeParentId`、撤回指定`withdraw`です。双方の相手・希望人数・所属先が一致した場合に成立します。18歳以上・生存・配偶者との矛盾なし・近親でないことをHarnessが確認します。初期既婚者も追加人数を指定できます。

希望人数は0〜4人で、既存実子と予約を含め各人・ペアの生涯上限4人です。結婚から第1子までと以降の出生間隔は1〜3年。runのseedと親ペアから抽選し、予定日を保存するため再起動で引き直しません。出生時に両親が男女ペア・18〜49歳・生存の条件を満たさなくなった場合は予約を取り消します。性別区分は男性/male、女性/female、その他で扱い、名前や外見から推測しません。

出生では親Conversationが新生児の初期条件を生成し、Harnessが検証、NPC Conversationの生成、記憶領域の追加、入場位置の確定まで行います。初期条件へ人生経験を付け足しません。世代は両親の最大世代＋1です。

## 新居と世帯

`createHome`は`members`と`description`を指定します。申請者は18歳以上で、本人が住まない未成年の代理申請もできます。成人の入居者は`consentHome`で本人が`requestId`と`accept`を指定します。同意・建築中の入居申請が重なる場合は拒否します。

住宅街Agentが`completeHome`で新居の領域と必要なサイズ拡張を返します。既存領域を動かさず、縮小・範囲外・家同士の重複を拒否します。承認済みの初期仕様は保持し、現在の住宅街だけを拡張します。空き家も残します。入居は所属世帯の変更であり、実際の移動はNPC自身が行います。

## 終了・保存

終了条件はターン上限、初期世代全員死亡、そのいずれかです。全住民死亡でも終了します。日次境界は加齢→死亡→出生→終了判定の順序で処理し、未完了の推論・配信・施設回答・記憶整理・Compactの解消を待ちます。

故人は活動と出生から除外し、Conversation・記憶・家族・関係の根拠を保存します。画面の故人一覧から履歴を開けます。死亡後とシミュレーション終了後のNPC端末は閲覧専用です。

申請・出生予定・外部生成操作・Compilation進捗を既存Workerへ保存します。外部操作前にdirtyを保存し、結果不明の生成を自動再送しません。出生が未確定の場合や制作要求の応答が不明な場合は正常終了扱いにせず、異常終了後は確定保存を閲覧専用で開きます。

## 自動Compilation

シミュレーション終了時に全生存NPCのCompilationを自動開始します。一時停止・エラー停止では開始しません。生存者0人なら推論しません。

仮プロンプトは`src/core/prompts/character-compiler.md`に分離し、`CompilerPromptProvider`から差し替えられます。親Conversationが本人のConversation、現在の保持記憶、方向別関係、本人に関連するイベントを読み、根拠付きの構造化結果を作ります。忘却した記憶や他人の非公開情報を本人の知識として復活させない方針です。

`compilation/<id>/npcs/<npcId>/`へ以下を出力します。

- `character.json`: 人物情報・人生の要約・人格・話し方・外見・目標
- `relationships.json`: 本人から相手への方向別認識
- `memories.json`: 選択された保持記憶
- `behavior.json`: 根拠のある行動とRuntime向け応答方針
- `schedule.json`: 根拠のある予定・習慣
- `system_prompt.md`: Runtime用人格プロンプト
- `review.json`: 設定と引用根拠をまとめた制作レビュー（アプリで検索・閲覧）
- `review.md`: 持ち出し用の制作レビューレポート
- `manifest.json`: 入力revision・入力hash・プロンプトhash・モデル・成果物hash

Schema・NPC参照・根拠参照を検証してからmanifestを確定します。採用した関係の根拠記憶は、同じNPC・同じrevisionでmemories.jsonにも含まれる必要があります。根拠記憶が出力対象にない関係は生成エラーとして表示し、記憶を自動で追加しません。文章と根拠の意味的な一致はモデルの生成品質に依存し、機械検証だけで保証しません。NPCごとの失敗と成功を表示し、成功済み出力を保持します。再生成ボタンは別の出力を作ります。Minecraft Modとスキン画像は含めません。

生成済みNPCの「制作レビュー」から、人格・話し方・行動・予定を検索し、各設定の根拠となる出来事・記憶・方向別関係・Conversationを確認できます。根拠はCompilation時点の保存資料です。Runtime向けの指針とPromptは観測された設定と区別して表示します。レビューは検証済み生成結果から機械的に作り、追加のモデル呼び出しは行いません。既存パッケージはそのまま閲覧でき、レビューはこの変更後に生成した出力から付きます。

「成果物を照合」、またはNPCフォルダーのmanifest.jsonを開くと、記録されたSHA-256と現在の各ファイルを照合します。欠損と内容変更を個別表示し、「もう一度照合」で再検査できます。照合は読取専用で、モデル接続は不要です。ファイルの種類やゲームエンジンによらずバイト列を比較します。manifest自体の真正性や、内容のゲーム上の意味は検証しません。

制作レビューの「別の出力と比較」では、同じワールド・同じNPCの別Compilationを基準に選べます。設定の追加・削除、同じ設定文の根拠変更、Runtime Promptや指針の変更を確認できます。引用資料の両側の本文も表示します。意味的な同一性・品質は判定せず、記憶・関係はレビューに記載した件数を比較します。ゲーム固有のデータ解釈や追加推論は行いません。

## 検証

`tests/backend/lifecycle.test.ts`は年齢・結婚・血縁・出生上限・取消・終了条件に加え、248ターンの複数世代シナリオで新居、出生、未成年の代理住宅申請、独立、死亡、追加NPCの保存復元を確認します。

`tests/backend/compiler.test.ts`は自動開始、全生存者生成、参照検証、部分失敗、再接続時の重複防止、再生成、対象0人、出生Sessionの追加と復元、親応答不明を確認します。`tests/e2e/lifecycle.spec.ts`は台帳、故人の閲覧、終了理由と生成済みパッケージの表示を確認します。

必須検証はREADMEのコマンドを使用します。今回追加したモデル生成はfixtureで検証し、実モデルの生成品質は別の受け入れ試験対象です。
