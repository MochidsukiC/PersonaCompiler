# Persona Compiler 改善・再評価メモ

> 以下は受領した提出版の評価記録です。対象フォルダー・Git管理状況・検証件数は提出時点のものです。

対象: `C:\Users\chika\Downloads\PersonaCompiler-main (1)\PersonaCompiler-main`

## 結論

既存設計を大幅に変えずに対応できる、クエスト連携周辺の致命的・高優先度問題は一巡して修正した。研究用プロトタイプから、ゲームアダプターを接続して統合試験へ進める基盤になった。

現時点の目安:

- NPCシミュレーション基盤: 75〜80%
- RPG向け設定・編集機能: 70%
- 実ゲーム統合可能なミドルウェア: 60〜65%
- 一般配布できる製品: 45〜55%

実ゲーム側アダプターと負荷試験は別リポジトリ・実環境が必要なため、このHarnessだけでは完了扱いにしていない。

## 今回修正した内容

### ゲーム本体との権限分離

- クエスト段階はゲーム本体だけが変更できる。
- 設定UIでは現在段階を読み取り専用にした。
- NPCおよび設定保存処理から段階・処理済みイベント・完了イベントを書き換えられないようにした。
- `source: game_adapter`のイベントだけを進行入口にした。
- 段階変更時、旧段階の保留directiveを取消すようにした。
- 実行中のdirective内容を編集した場合も古い発火状態を取消す。

理由: AI、編集画面、ゲーム本体の三者が同じ状態を書き換えると、進行の二重適用や巻き戻りが起きるため。

### 発火条件と即時結果

- 既定の発火条件を`player_interact`にした。
- `stage_enter`、`player_proximity`、`npc_arrival`、`explicit`も選択可能にした。
- NPC推論中でもゲームイベントを受け付け、LifeHarness内部で直列化するようにした。
- 発火結果を返却スナップショットの`questEventResult`で即時確認できるようにした。
- `spoken`、`queued`、`pending_location`、`already_active`、`ignored`、`expired`、`duplicate`などを区別した。
- 指定された`questId`と別クエストが優先度によって誤発火する問題を修正した。

理由: リアルタイムゲームでは「受け付けたか」「待機中か」「即時表示できるか」を同期的に判断する必要があるため。

### イベントID・重複・古い応答

- ゲームイベントIDによる冪等性を追加した。
- 処理失敗したイベントを処理済みにしないよう、ゲームイベント処理をロールバック可能にした。
- directiveの発火をactivation event ID単位で管理した。
- 半固定・自由生成のクエスト発話は`questActivationEventId`の一致を必須にした。
- 段階変更・取消・期限切れ後に、古いAI応答が到着しても発話を拒否する。
- 同じdirectiveの処理中に再発火して二重プロンプトを作らず、`already_active`または既存の場所待機を返す。

理由: 非同期AIでは、生成開始後にゲーム状態が変わることが普通にあり、古い結果を世界へ適用しない仕組みが必要なため。

### 固定台詞と完了イベント

- 場所条件を満たす固定台詞はAIを起動せずHarnessが直接発話する。
- 固定台詞をモデルが言い換えない。
- 発話ではクエストを自動進行しない。
- 発話後に完了イベントを生成し、ゲーム側が明示的に消費する。
- 完了イベントへ実際の台詞、発話方式、fallback使用有無、activation event IDを保存する。
- クエスト定義を削除した後も、既に発生した完了イベントを消費できるようにした。
- 完了イベントの重複登録を防止した。

理由: 重要台詞の完全保証と、台詞表示・ゲーム進行の責務を分離するため。

### 半固定台詞の検査

- 必須情報を構造化Factにした。
- FactはID、canonical value、言い換え可否、evidence termsを持つ。
- Fact IDの自己申告だけでは合格せず、実際の発話本文にcanonical valueまたは証拠語が必要になった。
- Unicode NFKC、大小文字、空白差を正規化して照合する。
- 許可された発話行為も検査する。
- 禁止情報も正規化後の本文で検査する。
- 検査失敗はactivation event単位で1回だけ再試行する。
- 2回目の失敗では設定済み固定fallbackを使用する。
- 完了イベントでfallback使用をゲーム側が判別できる。

理由: モデルがFact IDだけを申告する抜け道と、別の会話の失敗回数が混ざる問題を防ぐため。

### 場所固定

- NPCを自動転送しない。
- 通常の移動Toolを使う移動依頼を生成する。
- 最大待機ターンを設定できる。既定値は4。
- 同じdirectiveが再発火しても元の期限を延長しない。
- 期限切れ時にactivation、再試行情報、場所待機を取消す。
- ゲーム側へ`expired`を通知する。
- directive削除時に残った場所待機も掃除する。

理由: 無限保留、再発火による期限延長、削除済み設定の残留を防ぐため。

### 複数クエストとID整合性

- メイン、緊急、進行中サイド、サイド、日常の優先順位を実装した。
- 同点競合は暗黙選択せず設定エラーにした。
- クエストID、directive ID、同一directive内のFact ID重複を保存時に拒否する。
- UIでクエストを削除後に追加してもIDが重複しない採番へ変更した。

理由: 配列順や文字列順で偶然クエストが選ばれる非決定性をなくすため。

### UI・資料・テスト

- 発火条件、クエスト種別、場所、最大待機、Fact、fallbackを設定可能にした。
- 未消費の完了イベントを設定画面で確認可能にした。
- ゲームアダプター契約を`QUEST_GAME_ADAPTER.md`に記載した。
- 段階変更不可になった仕様へE2Eテストを更新した。
- Chromium/ANGLEのごく小さいアンチエイリアス差で不安定だったCanvasテストを、意味のある再描画は検出する許容幅へ修正した。

## 検証結果

- TypeScript型検査: 成功
- ESLint: 成功
- ユニットテスト: 38/38成功
- バックエンドテスト: 116/116成功
- Electron E2E: 13/13成功
- 本番ビルド: 成功

## 現時点の再評価

クエスト台詞については、固定・半固定・自由生成を安全に共存させる中核構造ができた。特に固定台詞の直接発話、activation ID、ゲーム権限、完了イベントの明示消費が揃ったため、以前の「名前付き設定だけ存在し、実行保証が弱い」段階は脱している。

一方で、Unity・Unreal等の実ゲームからイベントを送るアダプターそのものはまだない。したがって、現状は「ゲーム統合可能なHarness」であり、「導入するだけで任意ゲームですぐ動く完成SDK」ではない。

## 大幅改変になるため保留した候補

### 実ゲームSDK

- Unity C#パッケージ
- Unreal Engineプラグイン
- WebSocket/HTTPによるプロセス外イベントAPI
- 切断、再接続、イベント再送、セーブ同期

これはゲームエンジンと配布形態の選択が必要なため、Harnessへ推測で追加していない。

### 意味ベース検査

現在のFact保証はcanonical valueと編集者が指定したevidence termsによる決定的検査である。より自由な言い換えを保証するには、埋め込みまたは別モデルによる意味判定が必要になる。ただしコスト、遅延、誤判定、再現性が変わるため別機能として設計すべき。

### 禁止情報の強度分類

`hard_block`、`rewrite`、`warn`の段階化、およびワールド共通の情報公開ルールは有用。ただし既存の禁止情報モデルを広げるため今回は保留した。実装場所は各directiveの禁止情報表と、再利用可能なワールド共通知識ルールの二層が適切。

### 同一directiveの並行会話

現在は同じdirectiveのactivationを一つに限定し、再発火は`already_active`にする。MMOのように複数プレイヤーが同じNPCへ同時に話しかける場合は、player/session IDごとのactivationへ大幅変更が必要。

### 大規模負荷・品質検証

- 100人以上のリアルタイム運用
- 数千クエストの検索性能
- 10,000件を超える冪等イベント履歴
- 長期間の完了イベント消費漏れ
- 実モデルを使った台詞遅延とfallback率

現行の処理済みイベント履歴は直近10,000件に制限している。ゲーム側を永続的な正本とし、必要なら将来データベース化する。

### バージョン管理

対象フォルダーには`.git`がなく、変更差分・復旧・友人側とのマージに弱い。Git初期化や既存リポジトリとの統合は履歴方針を変えるため自動実施していないが、次の共同開発前に最優先で対応した方がよい。

## 次に進む推奨順

1. 友人側とゲームアダプター契約を確認する。
2. 使用予定のゲームエンジンを一つ選び、最小アダプターを作る。
3. プレイヤー会話・接近・NPC到着・セーブ復元の統合テストを作る。
4. 実モデルで遅延、fallback率、Fact失敗率を測る。
5. その結果を見て意味検査と禁止情報強度を追加する。
